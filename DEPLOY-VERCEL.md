# CyberShield V31 — pacote pronto para Vercel

Este pacote deve ser enviado para a raiz do repositório conectado ao Vercel.
O `index.html` permanece como entrada da aplicação e o `vercel.json` preserva as regras existentes.

A página Protege atualizada está em `playbook.html`, com `protect-v30.css` e `protect-v30.js`.
